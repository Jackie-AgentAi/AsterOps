# Grafana仪表板配置
apiVersion: 1

providers:
  - name: 'llmops-dashboards'
    orgId: 1
    folder: 'LLMOps'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
